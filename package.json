{
  "name": "probleemilahendaja",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm:dev:server\" \"npm:dev:client\"",
    "dev:client": "vite",
    "dev:server": "node --watch server.mjs",
    "backfill:content": "node server.mjs --backfill-content",
    "generate:persona-images": "node scripts/generate-persona-story-images.mjs",
    "start": "node server.mjs --production",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^7.1.9"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.102.1",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "openai": "^6.3.0"
  }
}
